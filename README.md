Markdown
# RankMind — Recruiter Intelligence System

An advanced hybrid AI-powered candidate discovery pipeline built for the Redrob Hackathon Challenge.

## 🚀 How to Run the Pipeline (CLI)
1. Install dependencies:
   ```bash
   pip install -r requirements.txt
Execute the engine over the candidate pool:

Bash
python rank.py --candidates ./data/candidates.jsonl --out ./RankMind.csv
🌐 Web Sandbox Interface
The visual web demo can be tested locally by running:

Bash
streamlit run app.py